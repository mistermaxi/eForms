@page
@model eForms.Pages.UploadFileModel
@{
    ViewData["breadcrumb2"] = "Upload Files";
}
<div class="container-fluid">
    <div class="animated fadeIn">
        <div class="row">
            <div class="col-md-6 col-lg-12">
                <h2>Upload to DB</h2>

                <form class="form-horizontal" method="post" enctype="multipart/form-data" asp-page-handler="Upload">
                    <div class="form-group row">
                        <!--<label class="col-md-2 col-form-label" for="file-input">Upload New Arrival Form</label>-->
                        <div class="col-md-3">
                            <input type="file" name="files" id="files" multiple />
                        </div>
                    </div>
                    <div class="form-group row">
                        <div class="col-md-3">
                            <input type="submit" value="Submit" class="btn btn-primary" />
                        </div>
                    </div>
                </form>

                <br /><br />
                @*<h2>Upload to Folder</h2>*@
            <h2>
                @*@ViewData["UploadStatus"].ToString()*@
            </h2>
                            @Html.Raw(ViewData["UploadStatus"].ToString())
                        
                <form class="form-horizontal" method="post" enctype="multipart/form-data" asp-page-handler="Upload2Folder">
                    <div class="form-group row">
                        <!--<label class="col-md-2 col-form-label" for="file-input">Upload New Arrival Form</label>-->
                        <div class="col-md-3">
                            <input type="file" name="files" id="files" />
                        </div>
                    </div>
                    <div class="form-group row">
                        <div class="col-md-3">
                            <input type="submit" value="Submit" class="btn btn-primary" />
                        </div>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>
          
                        

